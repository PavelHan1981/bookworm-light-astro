# pavelhan.tech — GSC 索引问题二次审计（2026-07-31）

> 复查 commit `f5c9f6f`（fix(seo): optimize sitemap filters, fix pagination maxPages and canonical tags）

## ✅ 已修复（方向正确，关键 bug 已解决）

| 项目 | 说明 |
|------|------|
| **分页空页 bug** | 原 `maxPages = Math.max(totalPages, 100)` 会生成 1–100 页，**超出实际文章数的页面全是空页** → 这是 177 重复 + 694 未索引的**主因之一**。已改为 `Math.max(实际页数, 1)`。✅ |
| **og:url canonical 拼接错误** | 原 `${base_url}/${pathname.replace("/", "")}` 误删首斜杠导致 og:url 错误；现统一改用 `canonical` 变量。✅ |
| **根分页页 noindex** | `/article/page/[...page]` 加 `noindex={true}`。✅ |
| **sitemap 排除** | 新增排除 `/search`、`/page/*`。✅ |
| **hreflang 配对** | 首页 + 文章详情页已实现完整 `zh` / `en` / `x-default`（→zh）配对。✅ |

## ⚠️ 仍需修复（3 项）

### 1. zh/en 分页页缺 noindex（中等）
- 文件：`src/pages/zh/article/page/[...page].astro`、`src/pages/en/article/page/[...page].astro`
- 现状：只加了 `canonical`，**没加 `noindex`**；而根 `/article/page` 已 noindex → 行为不一致
- 后果：`/zh/article/page/2` 等会被收录（sitemap 不排除 `/zh/...` 前缀），与列表首页产生重复信号
- 修复：在 `<Base ...>` 上补 `noindex={true}`（保留现有 canonical 指向第一页 / 自身即可）

### 2. 配对内容页缺 hreflang（中等，177 重复的剩余来源）
以下页面有 `/zh` 和 `/en` 版本但**无 hreflang 配对**：
- `/zh/about` ↔ `/en/about`
- `/zh/elements` ↔ `/en/elements`
- `/zh/privacy-policy` ↔ `/en/privacy-policy`
- （`/zh/page/[slug]` ↔ `/en/page/[slug]` 已 noindex，影响较小）
- 修复：仿照 `zh/index.astro` 的写法，在每个配对页 `<head>` slot 加 `zh`/`en`/`x-default` 三条 alternate 链接

### 3. 404 的 38 个（需 GSC 数据确认）
- 可能来源：旧 slug 迁移、外部死链、或之前分页超范围（现已修）
- 行动：GSC → 索引 → 页面 → 筛选「未找到 (404)」→ 导出 URL 列表；若是迁移旧 URL 则加 301 重定向

## 📌 架构确认（非问题）
- 根 `/article/*`、`/`（首页）是**语言重定向页**（noindex）→ sitemap 排除首页 `p===""` 属**正确**
- `tags/categories/authors` 在根目录（单一语言、混排中英文），标签链接 `/tags/xxx` 无语言前缀 → **无 404 风险**
- `trailing_slash: false` → URL 不带尾斜杠；canonical 均不带斜杠，一致 ✅

## 🔄 后续步骤（重要）
1. 重新 `astro build` + 部署
2. GSC → 站点地图 → 重新提交 `sitemap-index.xml`
3. **当前 GSC 数据仍是改版前的旧状态**，需等 1–2 周 Google 重新抓取后看新数据
4. 复查时重点看：重复网页数是否下降、404 是否减少、新提交的 sitemap 收录比
