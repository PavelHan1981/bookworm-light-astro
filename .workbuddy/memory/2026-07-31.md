## 2026-07-31 — GSC 索引问题 SEO 审计（二次复查）

用户 pavelhan.tech（Astro 静态站，中英双语 i18n）收到 GSC 索引异常邮件后，提交了修复 commit `f5c9f6f`。

### 已修复（✅ 方向正确）
- **分页空页 bug（关键）**：原 `maxPages = Math.max(totalPages, 100)` 会生成 1–100 页空分页页 → 大量重复/薄内容（177 重复 + 694 未索引主因之一）。改为 `Math.max(实际页数, 1)`。
- **og:url canonical 拼接错误**：原 `${base_url}/${pathname.replace("/", "")}` 误删首斜杠；现改用 canonical 变量。
- **根 `/article/page` 分页页加 noindex**；sitemap 排除 `/search`、`/page/*`。
- **hreflang 已实现**：首页 + 文章详情页有完整 zh/en/x-default 配对（x-default→zh）。

### 架构确认
- 根 `/article/*`、`/`（首页）是语言重定向页（noindex），真实内容在 `/zh/article`、`/en/article`。sitemap 排除首页 `p===""` 属正确（重定向页不应索引）。
- `tags/categories/authors` 在根目录（单一语言，混排中英文），标签链接 `/tags/xxx` 无语言前缀，无 404 风险。

### 仍需修复（⚠️ 已写入审计 artifact）
1. **zh/en 分页页缺 noindex** → ✅ 已在 commit `07cbab3` 修复（两分页页均加 `noindex={true}`，保留 canonical）。
2. **配对页缺 hreflang** → ✅ 已在 commit `b965c2c` 修复。
3. **404 的 38 个需 GSC 具体 URL** 才能对症（疑旧 slug 迁移或外链）。

### 最终审计结论（基于 07cbab3，未改代码）
- 代码层面索引结构性缺陷（R1 分页空页 / R2 canonical 拼接 / R3 分页 noindex / R4 配对 hreflang）**全部修复**，noindex / hreflang / sitemap 三者策略一致。
- 唯一剩余：404 的 38 个，需 GSC 导出具体 URL（代码层面已排除标签/重定向 404 风险）。
- 已输出正式审计报告：`E:\bookworm-light-astro\.workbuddy\artifacts\seo-audit-report-2026-07-31.md`

### 提醒
GSC 数据滞后，需重新 build+部署 → GSC 重提交 sitemap → 等 1–2 周看新数据。

## 2026-07-31（二）SEO + GEO 全面配置体检 — 产出 seo-geo-review-2026-07-31.md

用户要求除索引问题外，再全面 review 当前配置有哪些 SEO/GEO 可优化点（不动代码，仅出报告）。

### 已确认的优势（不动）
hreflang 完整配对(5对zh/en+x-default)；文章页 BlogPosting JSON-LD 质量高(author Person+sameAs, publisher Organization, dates)；llms.txt 体系完善(精选+226篇全量 ZH/EN 目录)；SimilarPosts 相关文章内部链接；remarkToc 自动目录；sitemap 排除=noindex 一致；RSS 三套；图片走 astro:assets 优化。

### 待优化（按优先级）
- P0：① `Base.astro:40` html lang 写死 zh-CN（EN 页也输出 zh-CN，影响语言定位/无障碍/GEO）；② og:type 永远 website(`Base.astro:233`)，文章页应是 article；③ 缺 Organization/WebSite/Person/BreadcrumbList schema（仅文章页有 jsonLd）。
- P1：④ MathJax 全站无条件加载(`Base.astro:111-135`)拖累 INP；⑤ 字体 Mulish 声明但未真正加载(无 @font-face/link，Font API 块被注释)→静默回退系统字体；⑥ sitemap 缺 lastmod；⑦ 缺 og:locale/alternate。
- P2(GEO)：⑧ 新增 llms-pillars.txt 嵌入核心全文；⑨ About 加 Person schema+外部档案；⑩ 支柱文章加 FAQPage；⑪ viewport maximum-scale=5 移除；⑫ 移除 meta generator；⑬ gtm_id 仍是占位符 GTM-XXXXXX 需核实分析生效；⑭ 图片 alt 规范。

### 关键发现备注
- EN 文章页只加 hreflang link，未改 html lang → 仍继承 Base 的 zh-CN（确认 bug）。
- 字体：theme.json 声明 Mulish，但全站无 googleapis/@font-face/fontsource 引用 → 当前无阻塞字体请求(对 CWV 无害)，但设计字体未生效。
- about 页无 JSON-LD（grep Person/jsonLd 在 about.astro 为空）。

## 2026-07-31（三）P0–P2 完整终轮审计 — seo-geo-final-audit-2026-07-31.md

用户对 P0/P1/P2 全部改完，要求终轮完整 review + 审计报告。改动目前在工作区（未提交）。

### 核验结论（逐行读实际代码，非看提交）
- **P0（3 项）全 ✅**：html lang 改 `computedLang`（/en→en，Base.astro:59-66）；og:type 改 ogType prop + article 元数据块（:261-276）；schema 全补——首页 WebSite+Organization+SearchAction、about Person+BreadcrumbList、文章 BlogPosting+Person+Organization+BreadcrumbList。
- **P1（4 项）全 ✅**：MathJax 改 `hasMath` 条件加载（Base:131-196）；字体改 Google Fonts link+preconnect+display=swap（:119-125）；sitemap lastmod 已加（功能达成，建议删行改用 git 自动）；og:locale/alternate 已加（:251-252）。
- **P2（7 项）全 ✅**：llms-pillars.txt 新增且全链路贯通（生成脚本→llms.txt→robots.txt）；about Person+sameAs(GitHub/官网/微信)；FAQPage schema 按 faq frontmatter 条件渲染(zh/en 对称);viewport 移除 maximum-scale;无 meta generator;GA4 `G-YGXYK8KCZN` 实开(真 ID 在采集),GTM enable=false 故 GTM-XXXXXX 占位无害。

### 非阻塞锦上添花（已写入报告）
1. 删 `Base.astro:13` 死代码 `import { Font }`
2. 删 `astro.config.mjs:23` lastmod:new Date() 改用 git 自动 per-page
3. 给 6 篇支柱文章补 faq frontmatter 让 FAQPage 真正生效
4. sameAs 补知乎/Scholar/LinkedIn 强实体
5. 正文 Markdown 图片 alt 内容巡检

### 下一步
提交 → astro build → 部署 → GSC 重提交 sitemap-index.xml → 等 1–2 周看新数据。GSC 仍是旧状态。
