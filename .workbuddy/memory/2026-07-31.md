## 2026-07-31 — GSC 索引问题 SEO 审计（二次复查）

用户 pavelhan.tech（Astro 静态站，中英双语 i18n）收到 GSC 索引异常邮件后，提交了修复 commit `f5c9f6f`。

### 已修复（✅ 方向正确）
- **分页空页 bug（关键）**：原 `maxPages = Math.max(totalPages, 100)` 会生成 1–100 页空分页页 → 大量重复/薄内容（177 重复 + 694 未索引主因之一）。改为 `Math.max(实际页数, 1)`。
- **og:url canonical 拼接错误**：原 `${base_url}/${pathname.replace("/", "")}` 误删首斜杠；现改用 canonical 变量。
- **根 `/article/page` 分页页加 noindex**；sitemap 排除 `/search`、`/page/*`。
- **hreflang 已实现**：首页 + 文章详情页有完整 zh/en/x-default 配对（x-default→zh）。

### 架构确认
- 根 `/article/*`、`/`（首页）是语言重定向页（noindex），真实内容在 `/zh/article`、`/en/article`。sitemap 排除首页 `p===""` 属正确（重定向页不应索引）。
- `tags/categories/authors` 在根目录（单一语言，混排中英文），标签链接 `/tags/xxx` 无语言前缀，无 404 风险。

### 仍需修复（⚠️ 已写入审计 artifact）
1. **zh/en 分页页缺 noindex**：`src/pages/zh|en/article/page/[...page].astro` 只加 canonical 未加 noindex，与根分页页不一致 → 会被收录产生重复。**（唯一剩余代码问题）**
2. **配对页缺 hreflang** → ✅ 已在 commit `b965c2c` 修复（about/elements/privacy-policy 的 zh/en 全加 hreflang + canonical，全站 5 对配对内容页覆盖完整）。
3. **404 的 38 个需 GSC 具体 URL** 才能对症（疑旧 slug 迁移或外链）。

### 提醒
GSC 数据滞后，需重新 build+部署 → GSC 重提交 sitemap → 等 1–2 周看新数据。
